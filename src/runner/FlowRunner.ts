// import Flow from '../flow/Flow'

export class FlowRunner {
    static run(): Promise<string> {

        return new Promise((resolve: Function, reject: Function) => {


            // resolve() - provide to channel

        })
    }
}
